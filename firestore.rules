// Copia y pega esto en Firebase Console > Firestore Database > Reglas
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /notas_remision/{notaId} {
      allow read, write: if request.auth != null;
    }
  }
}
